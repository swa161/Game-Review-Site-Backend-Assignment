meta {
  name: users id -invalid- No token
  type: http
  seq: 13
}

patch {
  url: {{BASE_URL}}/users/2
  body: json
  auth: none
}

headers {
  X-Authorization: token
  Content-Type: application/json
}

body:json {
  {
    "password": "new secure password",
    "currentPassword": "password"
  }
}

tests {
  test("Status code is 400, 401, 403, or 404", function () {
      expect(res.getStatus()).to.be.oneOf([400, 401, 403, 404]);
  });
}
