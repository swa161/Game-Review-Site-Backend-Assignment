meta {
  name: users id image -forbidden
  type: http
  seq: 5
}

delete {
  url: http://localhost:4941/api/v1/users/2/image
  body: none
  auth: none
}

tests {
  test("Status code is 403", function () {
      expect(res.getStatus()).to.equal(403);
  });
  
  
}
