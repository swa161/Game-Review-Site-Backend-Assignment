meta {
  name: games id owned -invalid- -no auth-
  type: http
  seq: 6
}

delete {
  url: http://localhost:4941/api/v1/games/5/owned
  body: none
  auth: none
}

tests {
  test("Status code is 401", function () {
      expect(res.getStatus()).to.equal(401);
  });
}
