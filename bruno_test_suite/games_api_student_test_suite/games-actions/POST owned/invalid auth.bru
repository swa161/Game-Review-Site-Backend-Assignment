meta {
  name: invalid auth
  type: http
  seq: 8
}

post {
  url: {{BASE_URL}}/games/10/owned
  body: none
  auth: none
}

headers {
  X-Authorization: NON-AUTH-TOKEN
}

tests {
  test("Status code is 401", function () {
      expect(res.getStatus()).to.equal(401);
  });
}
